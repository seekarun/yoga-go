// For now, the modern template uses the same photo gallery as classic
// This can be customized later for a more modern look
import PhotoGallerySection from '../../classic/sections/PhotoGallerySection';

export default PhotoGallerySection;
